
public class Boat {

	private int length;
	private int xPos;
	private int yPos;
	private int dir;
	
	public Boat(int l, int x, int y, int d) {
		checkParams(l, x, y, d);
	}

	private void checkParams(int l, int x, int y, int d) {
		if (l < 2 || l > 5) {
			System.out.println(Can't crate boat: illegal length);
		} else if 
		
	}
	
}
